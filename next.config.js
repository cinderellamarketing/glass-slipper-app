/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove deprecated appDir option (it's default in Next.js 14)
  // Remove env section (Next.js automatically loads .env.local)
}

module.exports = nextConfig